# Testing Environment Configuration
# This file is loaded when ENVIRONMENT=testing

# Force testing environment
ENVIRONMENT=testing

# Always use mock provider in tests
LLM__PROVIDER=mock

# Test data directory
DATA_DIR=./test_data

# Test secrets (not real)
FLASK_SECRET_KEY=test-secret-key-for-testing

# No real API keys needed for tests
# LLM__OPENAI_API_KEY=  # Not needed - using mock
# LLM__OPENROUTER_API_KEY=  # Not needed - using mock

